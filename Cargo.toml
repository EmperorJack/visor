[workspace]

members = [
  "cli",
  "display",
  "draw",
  "engine",
  "plugin",
  "plugin-draw",
  "runtime",
  "wgpu",
]

resolver = "2"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
