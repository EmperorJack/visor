[package]
name = "visor"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
visor_engine = { path = "../crates/visor_engine" }
visor_engine_builder = { path = "../crates/visor_engine_builder" }
