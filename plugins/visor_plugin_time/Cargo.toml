[package]
name = "visor_plugin_time"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
deno_core = "0.303.0"
visor_engine = { path = "../../crates/visor_engine" }
